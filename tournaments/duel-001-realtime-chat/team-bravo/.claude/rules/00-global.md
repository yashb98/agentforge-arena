# Global Rules
- Python 3.12+, strict typing, async-first
- Import order: stdlib → third-party (fastapi, pydantic) → local (src.)
- snake_case for functions, PascalCase for classes
- Every exception handler logs context before re-raising
